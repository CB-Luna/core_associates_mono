# ============================================================
# CORE ASSOCIATES - Variables de Entorno
# ============================================================
# Copia este archivo como .env y ajusta los valores

# ── PostgreSQL ──
DB_USER=core_admin
DB_PASSWORD=core_secret_2026
DB_NAME=core_associates_db
# Conexión desde host: localhost:5435
# Conexión desde Docker: postgres:5432

# ── Redis ──
REDIS_PASSWORD=core_redis_2026
# Conexión desde host: localhost:6382
# Conexión desde Docker: redis:6379

# ── MinIO ──
MINIO_ROOT_USER=core_minio_admin
MINIO_ROOT_PASSWORD=core_minio_secret_2026
# API desde host: localhost:9002
# Console desde host: localhost:9003
# Desde Docker: minio:9000

# ── JWT ──
JWT_SECRET=cambiar_en_produccion_usar_string_aleatorio_64_chars
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ── NextAuth ──
NEXTAUTH_SECRET=cambiar_en_produccion_usar_string_aleatorio_64_chars
NEXTAUTH_URL=http://localhost:3600

# ── Twilio (SMS OTP) ──
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+521XXXXXXXXXX

# ── Demo OTP bypass (lista de teléfonos separados por coma que aceptan 000000 en producción) ──
# Seed (ya tienen asociado creado): +525510000001 a +525510000003
# Registro nuevo (sin asociado, para demos de alta): +525520000001 a +525520000005
DEMO_PHONES=+525512345678,+525510000001,+525510000002,+525510000003,+525520000001,+525520000002,+525520000003,+525520000004,+525520000005,+525530000001,+525530000002,+525530000003,+525530000004,+525530000005

# ── Firebase Admin SDK (Push Notifications — FCM HTTP v1) ──
# Opción 1: Ruta al archivo JSON de service account
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Opción 2: JSON de service account en base64 (ideal para Docker/CI)
# FIREBASE_SERVICE_ACCOUNT_BASE64=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii...

# ── pgAdmin (opcional, puerto 5056 para no chocar con gen_portales en 5055) ──
PGADMIN_EMAIL=admin@coreassociates.com
PGADMIN_PASSWORD=admin123
