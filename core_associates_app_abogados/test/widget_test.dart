import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder — tests reales se agregan en fases posteriores
    expect(1 + 1, equals(2));
  });
}
